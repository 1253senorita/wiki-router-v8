<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!-- 첫 번째 텍스트: ID 추가 및 화면 중앙 기준 배치 -->

    <!-- 두 번째 텍스트: 첫 번째 텍스트 바로 아래에 배치 -->

    <TextView
        android:id="@+id/tv_hello_one"
        android:layout_width="138dp"
        android:layout_height="115dp"
        android:text="Hello World!"
        app:layout_constraintBottom_toTopOf="@+id/tv_hello_two"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.54"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="packed" />

    <TextView
        android:id="@+id/tv_hello_two"
        android:layout_width="134dp"
        android:layout_height="131dp"
        android:layout_marginTop="16dp"
        android:text="Hello Worlda!"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.571"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tv_hello_one" />

</androidx.constraintlayout.widget.ConstraintLayout>